/** GitHub Pages — синхронизация live-урока через live/session.json (работает без Supabase) */
window.GITHUB_RAW_BASE =
  "https://raw.githubusercontent.com/KamillaSnipes/English/main";

/** Supabase: если заполните — realtime вместо GitHub (Project Settings → API) */
window.SUPABASE_URL = "";
window.SUPABASE_ANON_KEY = "";
